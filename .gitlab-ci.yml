image:
  jakzal/phpqa

before_script:
    - cd api
    - composer install

cache:
  paths:
    - vendor/

stages:
  - phpstan

phpstan:
  stage: phpstan
  script:
    - phpstan analyse --level=1 src
  allow_failure: true